// default export - 1x pro file

export default class Car {
  constructor(type, name) {
    this.type = type;
    this.name = name;
  }
}

export const defaultName = 'Auto';

export const carTypes = ['Kombi', 'Cabrio', 'Bus'];
